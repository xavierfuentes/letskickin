<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="{{ url('landingpage') }}">
            {% image output="image/organiza-regalos-en-grupo-facil-seguro.png"
                '@letskickinFrontBundle/Resources/image/topbar/logo_header.png'
            %}
            <img src="{{ asset_url }}" alt="Bienvenido al crowdgifting"/>
            {% endimage %}
        </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
        <form id="topbar-subscribe" class="validate navbar-form navbar-right" role="subscribe" novalidate action="{{ mailchimp_form.action }}" method="{{ mailchimp_form.method }}">
            <div class="form-group">
                <input type="text" class="form-control required email" placeholder="tu@email.com" name="topbar-subscribe-email" id="topbar-subscribe-email">
            </div>
            <button type="submit" class="btn-default" name="topbar-subscribe-submit" id="topbar-subscribe-submit">Enviar</button>
        </form>
        <p class="navbar-text navbar-right text-help">
            Únete al #crowdgifting
            {% image output="image/unete-al-crowdgifting.png"
                '@letskickinFrontBundle/Resources/image/topbar/small_arrow.png'
            %}
            <img src="{{ asset_url }}" alt="Únete al crowdgifting"/>
            {% endimage %}
        </p>
    </div><!-- /.navbar-collapse -->
</nav>